name: Legacy Heroku CD (disabled)

on:
  workflow_dispatch:

jobs:
  heroku_cd_disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Heroku CD disabled
        run: |
          echo "Heroku CD is disabled."
          echo "Use the EC2 + Docker deployment flow instead."